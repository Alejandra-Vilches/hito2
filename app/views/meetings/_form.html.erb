<%= form_with(model: meeting) do |form| %>
  <% if meeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
        <% meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :place, style: "display: block" %>
    <%= form.text_field :place %>
  </div>

  <div>
    <%= form.label :activity, style: "display: block" %>
    <%= form.text_field :activity %>
  </div>

  <div>
    <%= form.label :time_date, style: "display: block" %>
    <%= form.date_field :time_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
